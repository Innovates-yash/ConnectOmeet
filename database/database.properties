# GameVerse Database Configuration
# MySQL Connection Settings

# Database Connection
db.host=localhost
db.port=3306
db.name=gameverse_db
db.username=root
db.password=gyash4841@

# Alternative configurations for different environments
# Development (current setup)
dev.db.host=localhost
dev.db.port=3306
dev.db.name=gameverse_db
dev.db.username=root
dev.db.password=gyash4841@

# Production (use environment variables in production)
prod.db.host=${DB_HOST:localhost}
prod.db.port=${DB_PORT:3306}
prod.db.name=${DB_NAME:gameverse_prod}
prod.db.username=${DB_USERNAME:root}
prod.db.password=${DB_PASSWORD:gyash4841@}

# Test Database (H2 in-memory for testing)
test.db.url=jdbc:h2:mem:gameverse_test;DB_CLOSE_DELAY=-1;MODE=MySQL
test.db.username=sa
test.db.password=

# Connection Pool Settings
db.pool.initial-size=5
db.pool.max-active=20
db.pool.max-idle=10
db.pool.min-idle=5
db.pool.max-wait=30000

# MySQL Specific Settings
db.driver=com.mysql.cj.jdbc.Driver
db.url=jdbc:mysql://${db.host}:${db.port}/${db.name}?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&characterEncoding=UTF-8

# Development Settings
db.show-sql=true
db.format-sql=true
db.hbm2ddl.auto=validate

# Production Settings (commented out for development)
# db.show-sql=false
# db.format-sql=false
# db.hbm2ddl.auto=none